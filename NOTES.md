# 作業メモ (2026-09-19 時点)

## 状態

| | C 版 (オス USB-C) | A 版 (オス USB-A) |
|---|---|---|
| 配線 | DRC エラー 0 / 未接続 0 | DRC エラー 0 / 未接続 0 |
| 確定盤 | `order/fanctl_c_routed_ok.kicad_pcb` | `order/fanctl_a_routed_ok.kicad_pcb` |
| 発注ファイル | `order/fanctl_c/` | `order/fanctl_a/` |
| 実装方式 | **Standard PCBA (強制)** | **Economic PCBA** |

基板は共通で 4 層 90 x 34mm、部品 105 点 / BOM 58 行。違いは J1 (PC 側プラグ) とその周辺だけ。

## JLCPCB 見積もり (各 5 枚 / 5 台・日本向け・2026-09-18 取得)

| 項目 | C 版 | A 版 |
|---|---|---|
| PCB | ¥1,091 | ¥1,093 |
| 初期費用 (Setup / ステンシル / 治具) | ¥7,825 | ¥1,516 |
| 部品代 (58 種) | ¥14,506 | ¥12,807 |
| 拡張部品フィーダー代 | ¥13,596 | ¥8,147 |
| 実装・手はんだ等 | ¥1,371 | ¥1,384 |
| **合計** | **¥38,466** | **¥24,947** |
| 送料目安 (UPS) | ¥1,486 | ¥1,486 |

## コストダウン案: C 版を Economic PCBA に載せる (約 1.3 万円減)

C 版が高いのは **J1 = TYPE-C-31-G-10 (C165948) が「Standard PCBA 専用部品」**のため。
これ 1 点のために実装方式が Standard に固定され、セットアップ費とフィーダー代が跳ね上がる。

- 置き換え候補: **TYPE-C-978-WFRP24 (C52741990)** — Economic 対応のオス USB-C プラグ
- 期待効果: A 版と同じ構造になるので **¥38,466 → ¥25,000 前後**
- 要作業: フットプリントが違うため **J1 周辺の再レイアウト + 再配線**
  (A 版で USB-A プラグを差し替えた時と同じ手順。`lib/fanctl.pretty/` に easyeda2kicad で取り込む)
- 判断待ち: やるかどうかは未決定。C 版を現状のまま発注しても動作上の問題はない。

## freerouting の罠 (この基板で踏んだもの)

1. **決定論的に動く。** `-mp` (パス数) を変えても同じ盤面・同じスコアになるので、
   「パス数を変えてリトライ」は意味がない。未接続が残るなら手配線するしかない。
2. **部品密集地帯を縦断する長い裏面配線を DSN に入れるとパス #4 以降でハングする。**
   この基板では x=163 付近 (R28-R31 / C23-C25 の列) の縦断が該当。x=166 にずらすと完走した。
   ハング判定は「200 秒で pass #4 に到達するか」で見れば 3 分で済む。
3. **手配線の検証に自動配線を回す必要はない。** 干渉は手配線同士で起きるので、
   `pcb.py <variant>` (ビルド) + `tools/drc.py` だけで約 90 秒で潰せる。
   自動配線は最後に 1 回で十分。
4. 長い手配線を入れたネットは `UNROUTED` に入れて自動配線から外す。銅箔は障害物としては効く。

## A 版で手配線に切り替えたネット

自動配線が抜けられず、毎回別のネットが落ちる状態だったため決め打ちにした:

- `I2C_SCL` / `I2C_SDA`: U1・MCU・プルアップ (R42/R43) が端から端に散る。2 本を平行レーンで縦断
- `ADC_IOUT` / `ADC_ILM_EXT`: MCU 左側 (0.4mm ピッチの ADC ピン) から基板東端まで。上辺を 2 レーンで横断し、
  交差する箇所だけ表面に上げて跨ぐ
- `QSPI_SD2`: フラッシュの下段ピンなので U7 の東を回って裏面で入れる
- `PC_FLT` / `EXT_OVLO`: 混雑域 (eFuse 周り / U2-J2 の隙間) からの脱出ビアを決め打ち
- `EXT_OVLO` は VBUS_EXT のビア列 (x=178.75 / 179.9 / 180.7) の隙間 x=179.32 を通す

## 次にやること

- [ ] C 版のコネクタ差し替えをやるか決める (上記コストダウン案)
- [ ] 発注 (カートには入れていない。支払いは本人判断)
